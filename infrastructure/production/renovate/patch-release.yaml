apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: renovate
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: renovate
              envFrom:
                - secretRef:
                    name: renovate-secrets
